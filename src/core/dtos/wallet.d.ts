export type WalletDetail = {
  id: number;
  name: string;
  initial_amount: string;
  icon: string;
  is_enabled: boolean;
};

export type WalletPayload = {
  name: string;
  initial_amount: string;
  // icon: number;
  // is_enabled: boolean;
};

